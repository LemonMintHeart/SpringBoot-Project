<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianshi.songzeyang.mapper.UserMapper">
    <!-- 结果集映射 -->
    <resultMap id="UserResultMap" type="User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
    </resultMap>

    <!-- 新增用户 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="user.id">
        INSERT INTO user (username, password)
        VALUES (#{user.username}, #{user.password})
    </insert>

    <!-- 根据ID删除用户 -->
    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <!-- 批量删除用户 -->
    <delete id="deleteByIds">
        DELETE FROM user WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 多条件更新 -->
    <update id="update">
        UPDATE user
        <set>
            <if test="user.username != null and user.username != ''">
                username = #{user.username},
            </if>
            <if test="user.password != null and user.password != ''">
                password = #{user.password}
            </if>
        </set>
        WHERE id = #{user.id}
    </update>

    <!-- 查询所有用户 -->
    <select id="selectAll" resultMap="UserResultMap">
        SELECT * FROM user ORDER BY id ASC
    </select>

    <!-- 用户名模糊查询 -->
    <select id="selectByUsernameLike" resultMap="UserResultMap">
        SELECT * FROM user
        WHERE username LIKE CONCAT('%', #{username}, '%')
    </select>

    <!-- 多条件精确查询 -->
    <select id="selectMultiCondition" resultMap="UserResultMap">
        SELECT * FROM user
        <where>
            <if test="user.id != null">
                AND id = #{user.id}
            </if>
            <if test="user.username != null and user.username != ''">
                AND username = #{user.username}
            </if>
            <if test="user.password != null and user.password != ''">
                AND password = #{user.password}
            </if>
        </where>
    </select>
</mapper>